# Pinned against actual verified install (tested 2026-06-23) -- these exact
# versions were used to confirm the Agent/AgentSession/@function_tool API
# surface this codebase targets. Bumping livekit-agents major versions may
# break the event hook names in livekit_agent.py -- re-verify against
# `python -c "import livekit.agents as la; print(dir(la))"` before upgrading.
livekit-agents==1.6.3
livekit-plugins-openai==1.6.3
livekit-api==1.1.0
fastapi==0.138.0
uvicorn[standard]>=0.29.0
httpx==0.28.1
supabase==2.31.0
pydantic==2.13.4
python-dotenv==1.2.2

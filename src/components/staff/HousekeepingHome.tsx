import HousekeeperPage from '@/pages/HousekeeperPage';

/**
 * Housekeeping home — wraps existing HousekeeperPage inside the Staff Shell.
 */
const HousekeepingHome = () => {
  return <HousekeeperPage embedded />;
};

export default HousekeepingHome;

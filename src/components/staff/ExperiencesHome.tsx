import ExperiencesPage from '@/pages/ExperiencesPage';

/**
 * Experiences home — wraps existing ExperiencesPage inside the Staff Shell.
 */
const ExperiencesHome = () => {
  return <ExperiencesPage embedded />;
};

export default ExperiencesHome;
